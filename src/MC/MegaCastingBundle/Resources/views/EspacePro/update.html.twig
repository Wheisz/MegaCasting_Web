{# views/EspacePro/update #}

{% extends '::base.html.twig' %}

{% block body %}
<div class="mainConteneur">
    <div class="titre_view">
        Espace pro - Mise à jour   
    </div>
    <div>
        {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
        
        {# Les erreurs générales du formulaire. #}
        {{ form_errors(form) }}        
        
        <div id="artiste">
            <div class="artiste_info">
                {% if type_info == "adresse" %} 
                    {% include 'MCMegaCastingBundle:Adresse:update.html.twig' %}
                {% elseif type_info == "general" %}
                    <div class="sous-titre">Informations société</div>
                    {% for error in errors %}
                    <div class="form-row">
                        <div class="form-error">{{ error }}</div>                  
                    </div>
                    {% endfor %}
                    {% include 'MCMegaCastingBundle:Annonceur:update.html.twig' %} 
                {% endif %}
                <div class="form-row">
                    {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
                    {{ form_widget(form.Sauvegarder, {'attr': {'class': 'bouton'}}) }}
                </div>
            </div>
        </div>        

        {# Génération automatique des champs pas encore écrits.
           Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
           et tous les champs cachés (type « hidden »). #}
        {{ form_rest(form) }}

        {# Fermeture de la balise <form> du formulaire HTML #}
        {{ form_end(form) }}
    </div>
</div>    
{% endblock %}